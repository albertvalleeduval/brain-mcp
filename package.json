{
  "name": "brain-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Remote MCP server + web cockpit for a private markdown second brain on GitHub, wired into claude.ai",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.8.1",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "agents": "^0.0.100",
    "hono": "^4.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "tsx": "^4.23.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.107.0"
  }
}
